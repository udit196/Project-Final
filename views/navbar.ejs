<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        #logoutBtn {
            display: block;
            position: absolute;
            right: 10px;
            top: 10px;
        }

        .navbar-nav .nav-link {
            padding: 0.8rem 1rem;
            /* Adjust padding for proper alignment */
            transition: background-color 0.3s ease;
            /* Smooth transition for hover effect */
            height: 100%;
            /* Ensure full height hover effect */
        }

        /* Highlight full navbar segment on hover */
        .navbar-nav .nav-link:hover {
            background-color: #003d80;
            /* Darker shade on hover */
            color: #ffffff;
            /* White text color */
        }

        /* Remove extra space caused by navbar */
        .navbar {
            padding: 0;
            /* Remove default padding of the navbar */
        }

        /* Fix logout button to the right */
        .navbar-nav {
            width: 100%;
            /* Make navbar-nav take up full width */
        }

        .navbar-nav .ms-auto {
            margin-left: auto;
            /* Align the logout button to the right */
        }
    </style>
</head>

<body>
    <header>
        <!-- <nav>
            <a href="/profile">Profile</a>
            <a href="/calculate">Calculate</a>
            <a href="/history">User-History</a>
            <a id="logoutBtn" href="/logout">Logout</a>
        </nav> -->
        <nav class="navbar navbar-expand-lg bg-primary">
            <div class="container-fluid">
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                    data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false"
                    aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
                    <div class="navbar-nav">
                        <a class="nav-link" aria-current="page" href="/profile">Profile</a>
                        <a class="nav-link" href="/calculate">Calculate</a>
                        <a class="nav-link" href="/history">User-History</a>
                        <a class="nav-link ms-auto" href="/logout">Logout</a>
                    </div>
                </div>
            </div>
        </nav>
    </header>
    <hr>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
</body>

</html>