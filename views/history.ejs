<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Previous Emissions History</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f8f9fa;
    }

    .container {
      margin-top: 20px;
    }

    .page-title {
      text-align: center;
      margin-bottom: 30px;
      color: #003d80;
    }

    .emission-card {
      background-color: #ffffff;
      border-radius: 8px;
      padding: 15px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      margin-bottom: 15px;
    }

    .emission-card h5 {
      margin-bottom: 10px;
      font-weight: bold;
      color: #003d80;
    }

    .emission-card p {
      margin: 0;
      font-size: 0.95rem;
      color: #555;
    }

    .details-link {
      color: #007bff;
      text-decoration: none;
      font-weight: bold;
    }

    .details-link:hover {
      text-decoration: underline;
    }
  </style>
</head>

<body>
  <%- include('navbar') %>
    <div class="container">
      <h2 class="page-title">Your Previous Carbon Emission Records</h2>
      <div class="row">
        <% history.slice().reverse().forEach(emission=> { %>
          <div class="col-md-6">
            <div class="emission-card">
              <h5>
                Date: 
                <%= new Date(emission.Timestamp).toLocaleDateString('en-IN', { 
                  year: 'numeric', 
                  month: 'long', 
                  day: 'numeric' 
                }) %>,
                Time: 
                <%= new Date(emission.Timestamp).toLocaleTimeString('en-IN', { 
                  hour: '2-digit', 
                  minute: '2-digit', 
                  hour12: true 
                }) %>
              </h5>
              <p><strong>Carbon Footprint:</strong>
                <%= emission.TotalYearlyEmissions %> kgCO₂
              </p>
              <a href="details/<%= emission.id %>" class="details-link">View Details</a>
            </div>
          </div>
          <% }) %>
      </div>
    </div>
</body>

</html>